# Weather-App-Aethera
Weather Forecast or Experience
C:\Weather App
├── icon.png          (App icon / 3D glassmorphic logo asset)
├── index.html        (Main dashboard layout structure & splash loader screen)
├── manifest.json     (PWA configuration schema for standalone device installations)
├── script.js         (Geocoding/Weather fallbacks, Autocomplete suggestions, Timezone & Canvas logic)
├── style.css         (Glassmorphic styles, Dark/Light mode tokens, and Widget visual systems)
└── sw.js             (Service Worker PWA cache controller with Network-First logic)
